[package]
name = "desk_apps"
version = "0.1.0"
edition = "2021"
description = "Ring-3 port of src/userspace/apps/desktop_apps.c — app windows, z-order, drag"

[dependencies]
desk_display = { path = "../desk_display" }
desk_calc = { path = "../desk_calc" }
desk_clock = { path = "../desk_clock" }
desk_calendar = { path = "../desk_calendar" }
desk_datetime = { path = "../desk_datetime" }

[lib]
crate-type = ["rlib"]
