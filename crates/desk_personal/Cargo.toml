[package]
name = "desk_personal"
version = "0.1.0"
edition = "2021"
description = "Ring-3 port of src/userspace/personalization.c — themes from /config/appear.ini"

[dependencies]
purec = { path = "../purec" }

[lib]
crate-type = ["rlib"]
