[package]
name = "desk_calc"
version = "0.1.0"
edition = "2021"
description = "Ring-3 port of src/userspace/apps/calculator_app.c"

[dependencies]
purec = { path = "../purec" }
desk_display = { path = "../desk_display" }

[lib]
crate-type = ["rlib"]
